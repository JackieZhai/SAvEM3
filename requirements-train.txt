# SAvEM3 / SAEM² 训练环境（Python 3.9）
# 说明：
#   1. 先按平台安装 PyTorch：
#      - Apple Silicon: pip install torch torchvision
#      - Linux CUDA:    按 https://pytorch.org 选择 CUDA 版本
#   2. 再安装本文件其余依赖。
#   3. waterz/elf 属于训练中验证与后处理依赖，不在本文件；见 repro/env/。

torch>=2.1
torchvision>=0.16
numpy>=1.26,<2.1
scipy>=1.10
scikit-image>=0.22
scikit-learn>=1.3
attrdict>=2.0
tensorboardX>=2.6
h5py>=3.10
PyYAML>=6.0
tqdm>=4.66
matplotlib>=3.8
opencv-python-headless>=4.9
pillow>=10.0
imageio>=2.34
tifffile>=2024.2
cloud-volume>=8.13
cloud-files>=3.0
connected-components-3d>=3.16
mahotas>=1.4
fastremap>=1.14
edt>=2.3
fastmorph>=1.7
joblib>=1.3
networkx>=3.0
